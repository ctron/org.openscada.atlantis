<project name="Build specific targets and properties" default="noDefault">

  <import file="tools.xml" />

  <!-- ===================================================================== -->
  <!-- Run a given ${target} on all elements being built -->
  <!-- Add on <ant> task for each top level element being built. -->
  <!-- ===================================================================== -->
  <property name="allElementsFile" value="${builder}/allElements.xml"/>
  <import file="${allElementsFile}" />
  <target name="allElements">
    <antcall target="allElementsDelegator" />
  </target>
  
  <!-- ===================================================================== -->
  <!-- ===================================================================== -->
  <target name="getBaseComponents" depends="checkLocalBase" unless="skipBase">
    <get src="${eclipseBaseURL}" dest="${buildDirectory}/../temp-base.zip" />
    <unzip dest="${base}" overwrite="true" src="${buildDirectory}/../temp-base.zip" />
  </target>

  <target name="checkLocalBase">
    <available file="${base}" property="skipBase" />
  </target>

  <!-- ===================================================================== -->
  <!-- Check out map files from correct repository -->
  <!-- Replace values for mapsCheckoutTag as desired. -->
  <!-- ===================================================================== -->
  <target name="getMapFiles" depends="checkLocalMaps" unless="skipMaps">
    <property name="mapsCheckoutTag" value="HEAD" />
    <cvs cvsRoot="${mapsRepo}" package="${mapsRoot}" dest="${buildDirectory}/maps" tag="${mapsCheckoutTag}" />
  </target>

  <target name="checkLocalMaps">
    <available property="skipMaps" file="${buildDirectory}/maps" />
  </target>

  <target name="tagMapFiles" if="tagMaps">
    <cvs dest="${buildDirectory}/maps/${mapsRoot}" command="tag ${mapsTagTag}" />
  </target>

  <!-- ===================================================================== -->

  <target name="clean" unless="noclean">
    <antcall target="allElements">
      <param name="target" value="cleanElement" />
    </antcall>
  </target>

  <target name="gatherLogs">
    <mkdir dir="${buildDirectory}/${buildLabel}/compilelogs" />
    <antcall target="allElements">
      <param name="target" value="gatherLogs" />
    </antcall>
    <unzip dest="${buildDirectory}/${buildLabel}/compilelogs" overwrite="true">
      <fileset dir="${buildDirectory}/features">
        <include name="**/*.log.zip" />
      </fileset>
    </unzip>
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do before setup -->
  <!-- ===================================================================== -->
  <target name="preSetup">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do after setup but before starting the build proper -->
  <!-- ===================================================================== -->
  <target name="postSetup">
    <antcall target="getBaseComponents" />
  	<antcall target="checkoutSvn">
  		<param name="todir" value="${buildDirectory}/plugins/org.openscada.rcp.update"/>
  		<param name="module" value="${svn.base}/rcp/update_plugin"/>
  	</antcall>
  	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/org.openscada.da.client.test"/>
 		<param name="module" value="${svn.base}/da/client/test"/>
 	</antcall>
  	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/features/org.openscada.da.client.test"/>
 		<param name="module" value="${svn.base}/da/client/test_feature"/>
 	</antcall>
  	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/features/org.openscada.da.net.client"/>
 		<param name="module" value="${svn.base}/da/client/net_feature"/>
 	</antcall>
  	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/log4j_wrapper_plugin"/>
 		<param name="module" value="${svn.ext.base}/log4j_wrapper_plugin"/>
 	</antcall>
  	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/ext_xmlbeans_plugin"/>
 		<param name="module" value="${svn.ext.base}/xmlbeans_plugin"/>
 	</antcall>
  	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/org.openscada.da.net.client"/>
 		<param name="module" value="${svn.base}/da/client/net_wrapper_plugin"/>
 	</antcall>
	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/org.openscada.da.rcp.LocalTestServer"/>
		<param name="module" value="${svn.base}/da/rcp/local_test_server"/>
	</antcall>
	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/org.openscada.rcp.client"/>
		<param name="module" value="${svn.base}/rcp/client"/>
	</antcall>
	<antcall target="checkoutSvn">
		<param name="todir" value="${buildDirectory}/features/org.openscada.rcp.client"/>
		<param name="module" value="${svn.base}/rcp/client_feature"/>
	</antcall>
	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/org.openscada.ae.net.client"/>
		<param name="module" value="${svn.base}/ae/client/net_wrapper_plugin"/>
	</antcall>
	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/org.openscada.ae.client.test"/>
		<param name="module" value="${svn.base}/ae/client/test"/>
	</antcall>
	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/features/org.openscada.ae.client.test"/>
		<param name="module" value="${svn.base}/ae/client/net_feature"/>
	</antcall>
	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/org.openscada.ae.rcp.LocalTestServer"/>
		<param name="module" value="${svn.base}/ae/rcp/local_test_server"/>
	</antcall>
	<antcall target="checkoutSvn">
 		<param name="todir" value="${buildDirectory}/plugins/org.openscada.da.client.viewer"/>
		<param name="module" value="${svn.base}/da/client/viewer"/>
	</antcall>
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do before fetching the build elements -->
  <!-- ===================================================================== -->
  <target name="preFetch">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do after fetching the build elements -->
  <!-- ===================================================================== -->
  <target name="postFetch">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do before generating the build scripts. -->
  <!-- ===================================================================== -->
  <target name="preGenerate">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do after generating the build scripts. -->
  <!-- ===================================================================== -->
  <target name="postGenerate">
    <antcall target="clean" />
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do before running the build.xmls for the elements being built. -->
  <!-- ===================================================================== -->
  <target name="preProcess">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do after running the build.xmls for the elements being built. -->
  <!-- ===================================================================== -->
  <target name="postProcess">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do before running assemble. -->
  <!-- ===================================================================== -->
  <target name="preAssemble">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do after  running assemble. -->
  <!-- ===================================================================== -->
  <target name="postAssemble">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do before running package. -->
  <!-- ===================================================================== -->
  <target name="prePackage">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do after  running package. -->
  <!-- ===================================================================== -->
  <target name="postPackage">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do after the build is done. -->
  <!-- ===================================================================== -->
  <target name="postBuild">
    <antcall target="gatherLogs" />
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do to test the build results -->
  <!-- ===================================================================== -->
  <target name="test">
  </target>

  <!-- ===================================================================== -->
  <!-- Steps to do to publish the build results -->
  <!-- ===================================================================== -->
  <target name="publish">
  </target>

  <!-- ===================================================================== -->
  <!-- Default target                                                        -->
  <!-- ===================================================================== -->
  <target name="noDefault">
    <echo message="You must specify a target when invoking this file" />
  </target>

</project>